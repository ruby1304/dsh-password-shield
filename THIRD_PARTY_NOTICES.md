# Third-party notices

The published runtime has no third-party runtime dependencies.

Development and test dependencies are recorded exactly in `package-lock.json`; their license metadata is available from the npm registry and the installed packages. DSH is an external host and is not bundled in this package.
